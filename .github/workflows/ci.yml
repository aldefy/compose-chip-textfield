name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build library
        run: ./gradlew :chip-textfield:build

      - name: Run tests
        run: ./gradlew :chip-textfield:allTests

      - name: API compatibility check
        run: ./gradlew :chip-textfield:apiCheck

      - name: Build sample (Android + Desktop)
        run: ./gradlew :sample:assembleDebug :sample:desktopJar
